# @package _group_
optuna_config:
  direction: maximize
  study_name: cifar10-alpha-deltaT-${masking.sparse_init}-density-${masking.density}
  storage: sqlite:///outputs/databases/cifar10_alpha_deltaT.db
  n_trials: 15
  n_jobs: 3
  sampler: tpe
  seed: 123